<x-layouts.app>
    <div>
        <p class="text-sm" style="color: var(--gc-text-soft);">{{ $section }}</p>
        <h1 class="mt-1 text-2xl font-semibold" style="color: var(--gc-text);">{{ $title }}</h1>
        <p class="mt-3 text-sm" style="color: var(--gc-text-soft);">{{ $description }}</p>
    </div>
</x-layouts.app>
